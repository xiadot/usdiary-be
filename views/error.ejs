<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>오류 발생</title>
</head>
<body>
  <h1>오류 발생</h1>
  <p><%= message %></p>
  <% if (error.status) { %>
    <p>상태 코드: <%= error.status %></p>
  <% } %>
  <% if (error.stack) { %>
    <pre><%= error.stack %></pre>
  <% } %>
</body>
</html>
